// 🔓 TEMPLATE - Este archivo SÍ se sube a GitHub (sin credenciales reales)
// Copia este archivo como 'environment.ts' y completa con tus credenciales

export const environment = {
  production: true,

  // 👇 Reemplaza estos valores con tus credenciales de Supabase
  // Obtén estas credenciales en: https://supabase.com → Tu Proyecto → Settings → API

  supabaseUrl: 'TU_URL_DE_SUPABASE', // Ejemplo: https://xxxxxx.supabase.co
  supabaseKey: 'TU_ANON_KEY_DE_SUPABASE', // Ejemplo: eyJhbGc...
};

/* 
📝 INSTRUCCIONES:

1. Copia este archivo:
   $ cp environment.template.ts environment.ts

2. Abre environment.ts y pega tus credenciales reales

3. NUNCA subas environment.ts a GitHub (ya está en .gitignore)

4. SIEMPRE sube environment.template.ts a GitHub (para que otros sepan qué configurar)
*/
